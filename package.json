{
  "name": "valoro-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --filter=valoro-next --filter=@valoro/docs",
    "lint": "turbo run lint",
    "storybook": "turbo run storybook",
    "build-storybook": "turbo run build-storybook",
    "ui:dev": "pnpm --filter @valoro/ui dev",
    "ui:build": "pnpm --filter @valoro/ui build",
    "ui:lint": "pnpm --filter @valoro/ui lint",
    "docs:dev": "pnpm --filter @valoro/docs storybook",
    "docs:build": "pnpm --filter @valoro/docs build-storybook",
    "docs:lint": "pnpm --filter @valoro/docs lint",
    "app:lint": "pnpm --filter valoro lint",
    "next:dev": "pnpm --filter valoro-next dev",
    "next:build": "pnpm --filter valoro-next build",
    "next:start": "pnpm --filter valoro-next start",
    "next:lint": "pnpm --filter valoro-next lint"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.12",
    "turbo": "^1.10.12",
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
